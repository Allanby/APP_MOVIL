<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Admin_RH"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- 👇 LÍNEA ELIMINADA 👇 -->
        <!-- android:usesCleartextTraffic="true" -->

        <activity
            android:name="com.example.admin_rh.MainActivity"
            android:exported="true"/>

        <activity android:name="com.example.admin_rh.RolesSeleccionActivity"
            android:exported="true"/>

        <activity android:name="com.example.admin_rh.NominaActivity"
            android:exported="true"/>

        <activity android:name="com.example.admin_rh.GerenciaActivity"
            android:exported="true"/>
        <activity android:name="com.example.admin_rh.ui.rh.RhMenuActivity"
            android:exported="true"/>



        <activity
            android:name="com.example.admin_rh.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
